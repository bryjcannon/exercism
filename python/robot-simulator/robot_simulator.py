class Robot(object):
    
    def __init__(bearing=NORTH, x_coord=0, y_coord=0):
        self.bearing = bearing
        self.coordinates = (x_coord, y_coord)
        
    def coordinates()
        return self.coordinate
        
    def bearing():
        return self.bearing
    
    def advance():
        if self.bearing == NORTH:
            self.coordinates = (x_coord, y_coord += 1)
        if self.bearing == SOUTH:
            self.coordinates = (x_coord, y_coord -= 1)
        if self.bearing == EAST:
            self.coordinates = (x_coord += 1, y_coord)
        if self.bearing == WEST:
            self.coordinates = (x_coord -= 1, y_coord)
        
    def turn_left():
        if self.bearing == NORTH:
            self.coordinates = (x_coord, y_coord += 1)
        if self.bearing == SOUTH:
            self.coordinates = (x_coord, y_coord -= 1)
        if self.bearing == EAST:
            self.coordinates = (x_coord += 1, y_coord)
        if self.bearing == WEST:
            self.coordinates = (x_coord -= 1, y_coord)
        
    def turn_right():
        if self.bearing == NORTH:
            self.coordinates = (x_coord, y_coord += 1)
        if self.bearing == SOUTH:
            self.coordinates = (x_coord, y_coord -= 1)
        if self.bearing == EAST:
            self.coordinates = (x_coord += 1, y_coord)
        if self.bearing == WEST:
            self.coordinates = (x_coord -= 1, y_coord)
